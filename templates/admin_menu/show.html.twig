{% extends "base.html.twig" %}

{% block title %}Menu page{% endblock %}
{% block title_header %}Menu{% endblock %}

{% block content %}

<main style="min-height: 75vh" class="container">
      <h1>Menu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ menu.name }}</td>
            </tr>
            <tr>
                <th>Starter</th>
                <td>{{ menu.starter }}</td>
            </tr>
            <tr>
                <th>Dish</th>
                <td>{{ menu.dish }}</td>
            </tr>
            <tr>
                <th>Dessert</th>
                <td>{{ menu.dessert }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ menu.price }}€</td>
            </tr>
        </tbody>
    </table>

    <a class="m-1 btn btn-warning" href="{{ path('app_admin_menu_edit', {'id': menu.id}) }}">edit</a>
    {{ include('admin_menu/_delete_form.html.twig') }}
</main>

{% endblock content %}
