{% extends "base.html.twig" %}

{% block title %}Login page{% endblock %}
{% block title_header %}Login{% endblock %}

{% block content %}

<main style="min-height: 75vh" class="container">
      <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ article.name }}</td>
            </tr>
            <tr>
                <th>Entrée</th>
                <td>{{ article.startDish }}</td>
            </tr>
            <tr>
                <th>Plat principal</th>
                <td>{{ article.mainDish }}</td>
            </tr>
            <tr>
                <th>Dessert</th>
                <td>{{ article.desert }}</td>
            </tr>
        </tbody>
    </table>

    <a class="m-1 btn btn-warning" href="{{ path('app_admin_article_edit', {'id': article.id}) }}">edit</a>
    {{ include('admin_article/_delete_form.html.twig') }}
</main>

{% endblock content %}
